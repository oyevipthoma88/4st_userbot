{
  "name": "4ST Prime Core — Telegram Music Userbot",
  "description": "Telethon + Pyrogram/PyTgCalls Telegram music userbot with zero-disk streaming, Invidious/Piped/JioSaavn/SoundCloud failover, and live GitHub-backed persistence.",
  "repository": "https://github.com/thomas82822/4st_userbot",
  "keywords": [
    "telegram",
    "userbot",
    "music",
    "pyrogram",
    "telethon",
    "pytgcalls"
  ],
  "stack": "heroku-22",
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "standard-2"
    }
  },
  "env": {
    "TELEGRAM_API_ID": {
      "description": "Telegram API ID from https://my.telegram.org (API development tools).",
      "required": true
    },
    "TELEGRAM_API_HASH": {
      "description": "Telegram API hash from https://my.telegram.org (API development tools).",
      "required": true
    },
    "TELEGRAM_BOT_TOKEN": {
      "description": "Bot token for the assistant/menu bot, from @BotFather.",
      "required": true
    },
    "TELEGRAM_PRIMARY_SESSION": {
      "description": "Telethon session string for the primary/owner userbot account (generate once locally, never share it).",
      "required": true
    },
    "TELEGRAM_PYRO_SESSION": {
      "description": "Pyrogram session string for the primary/owner account's music engine. Leave blank and set it up later from the bot's Music Setup menu if you don't have one yet.",
      "required": false
    },
    "TELEGRAM_OWNER_ID": {
      "description": "Your numeric Telegram user ID (owner/admin of this bot).",
      "required": true
    },
    "TELEGRAM_OWNER_USERNAME": {
      "description": "Your Telegram @username (without the @), used for owner-recognition fallback.",
      "required": false
    },
    "TELEGRAM_LOG_CHANNEL_ID": {
      "description": "Numeric chat ID of a private channel/group the bot logs deploy/error events to.",
      "required": false
    },
    "TELEGRAM_HELP_LINK": {
      "description": "t.me link for the 'Help/Report' button on the /start UI.",
      "required": false
    },
    "GEMINI_API_KEY": {
      "description": "Google Gemini API key, used for any AI-assisted bot features.",
      "required": false
    },
    "GITHUB_TOKEN": {
      "description": "GitHub PAT with contents:write on GITHUB_REPO. Used ONLY to permanently store the /start visual media (start picture) so it survives dyno restarts. Config backups still go to MongoDB.",
      "required": false
    },
    "GITHUB_REPO": {
      "description": "owner/repo where the start picture is committed (e.g. oyevipthoma88/4st_userbot).",
      "required": false
    },
    "GITHUB_BRANCH": {
      "description": "Branch used for the start-picture commit.",
      "value": "main",
      "required": false
    },
    "MONGO_URI": {
      "description": "MongoDB connection string (Atlas: mongodb+srv://...). Stores config/sessions — replaces the old GitHub backup.",
      "required": false
    },
    "MONGO_DB": {
      "description": "MongoDB database name.",
      "required": false,
      "value": "4st_userbot"
    },
    "YOUTUBE_API_KEY": {
      "description": "YouTube Data API v3 key — fast search for .play/.vplay (comma-separated for multiple keys). Optional.",
      "required": false
    },
    "GITHUB_MEDIA_DIR": {
      "description": "Folder inside the repo for stored media.",
      "value": "media",
      "required": false
    },
    "TELEGRAM_SUPPORT_LINK": {
      "description": "t.me link for the 'Support' button on the /start UI (optional — button hidden if unset).",
      "required": false
    },
    "TELEGRAM_UPDATES_LINK": {
      "description": "t.me link for the 'Updates' button on the /start UI (optional — button hidden if unset).",
      "required": false
    },
    "WORDSEEK_BOT_LINK": {
      "description": "t.me link to the WordSeek cheat bot, shown as a cross-promo button on /start (optional).",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku-community/apt"
    },
    {
      "url": "heroku/python"
    }
  ]
}
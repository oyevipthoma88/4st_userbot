# Termux (Android) dependency set.
# pytgcalls / ntgcalls / pyrofork / tgcrypto need native toolchains that
# Termux cannot reliably build, so the music (voice-chat) engine is skipped.
# main.py detects Termux and disables music commands automatically.

telethon==1.44.0
yt-dlp>=2025.1.1
google-genai>=0.1.0
aiohttp>=3.9.0
aiofiles>=23.0.0
pillow>=10.0.0
pycryptodome>=3.18.0
python-dotenv>=1.0.0
requests>=2.31.0
pymongo>=4.9,<5
